#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "$SCRIPT_DIR/common.sh"

ghidra_re_notes_init_files

python3 "$(ghidra_re_notes_backend)" status \
  --config-file "$GHIDRA_NOTES_CONFIG_FILE" \
  --state-file "$GHIDRA_NOTES_STATE_FILE" \
  --queue-dir "$GHIDRA_NOTES_QUEUE_DIR" \
  --cache-json "$GHIDRA_NOTES_CACHE_JSON" \
  --cache-md "$GHIDRA_NOTES_CACHE_MD"
